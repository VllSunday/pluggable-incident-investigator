image: python:3.12-slim

stages:
  - test

test:
  stage: test
  before_script:
    - python -m pip install --disable-pip-version-check pytest==8.4.2 ruff==0.12.12
  script:
    - python -m pytest -q
    - ruff check .
